<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Console output, standard -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Ton fichier de log dédié -->
  <appender name="DISCORD_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>logs/discord-events.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <!-- Nouveau fichier chaque jour -->
      <fileNamePattern>logs/discord-events.%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>

    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ROOT logger (stdout par défaut) -->
  <root level="INFO">
    <appender-ref ref="CONSOLE" />
  </root>

  <!-- Logger spécifique pour ta classe -->
  <logger name="Golem.api.security" level="INFO" additivity="false">
    <appender-ref ref="DISCORD_FILE" />
    <appender-ref ref="CONSOLE" />
  </logger>

</configuration>
